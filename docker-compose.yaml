services:
  db:
    image: postgres:14.21-alpine
    container_name: db
    ports:
      - 5432:5432
    environment:
    - POSTGRES_USER=admin
    - POSTGRES_PASSWORD=passwd
    - POSTGRES_DB=taskmgmt
    
